# Pandoc Template

### Features

* Multipage documents
* Lua Filter to use meta props as macros within markdown files with `{{prop}}` syntax
* `./src/*.md` --> `./docs/*.pdf`


### Build 

* pandoc
* texlive
* xelatex

```
make
```

